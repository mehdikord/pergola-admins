import Authentication from "@/views/auth/Authentication.vue";

const auth = [
    {
        path:'/authentication',
        name:'auth',
        component:Authentication,
    }


]

export default auth;